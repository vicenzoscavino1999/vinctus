# Metrics baseline (Phase 0)

All metrics must include a capture method so they can be repeated later.

## Cost baseline (Firebase)

- Capture date:
- Firestore:
  - Monthly cost:
  - Reads/writes (console):
  - Method (example): Firebase Console > Usage, export screenshot/link.
- Functions:
  - Monthly cost:
  - Invocations:
  - Method:
- Storage:
  - Monthly cost:
  - Stored GB / egress:
  - Method:
- Auth:
  - Monthly cost:
  - MAU:
  - Method:

## Latency baseline (perceived)

- Login:
  - Time:
  - Method (example): stopwatch from click to first screen render.
- Feed open:
  - Time:
  - Method:
- Chat open:
  - Time:
  - Method:

## Bundle size baseline

- Build output (report link/file):
- Total size:
- Method (example): `npm run build` + analyzer report.

## Test coverage baseline

- Unit/integration coverage:
- E2E coverage:
- Method (example): Vitest coverage + Playwright report.

## Per-action reads/calls (dev logs)

- Open feed:
  - Reads/calls:
  - Method (example): dev logs + Firebase emulator logs.
- Open chat:
  - Reads/calls:
  - Method:
- Create post:
  - Reads/calls:
  - Method:
